from __future__ import annotations

import argparse
import zipfile
from pathlib import Path
from xml.etree import ElementTree as ET

from docx_reference_merge import NS, body_paragraphs, para_text, replace_para_text_keep_style


ET.register_namespace("w", NS["w"])


REWRITES = {
    59: "心理健康是大学生顺利完成学业和适应校园生活的重要基础。近几年，学习任务、人际交往、就业竞争和家庭环境等压力常常交织在一起，一些学生因此出现焦虑、抑郁、情绪低落、睡眠障碍或适应困难等情况。高校心理健康教育与咨询工作不能只停留在单次谈话，还要覆盖知识宣传、心理测评、预约咨询、危机预警、持续跟踪和管理统计等环节。相比之下，传统线下预约、人工记录和单点沟通的方式，容易受时间、场地和人员数量限制，服务连续性也不够稳定[1-4]。",
    60: "随着高校信息化建设不断推进，心理健康服务也逐渐从分散的线下流程转向线上协同。基于 Web 的服务平台可以把心理测评、咨询预约、科普阅读和在线沟通放在同一入口中。学生使用时不必反复寻找渠道，求助顾虑会有所降低；咨询师可以集中处理预约、查看沟通记录并跟踪服务对象；管理员也能通过后台汇总运行数据、风险分布和近期动态。这样一来，心理服务的组织效率和管理精度都会得到提升[5-7]。",
    200: "系统架构既要方便毕业设计阶段的开发调试，也要为后续维护留下空间。前端项目构建后可作为静态资源部署，后端以 Spring Boot 服务形式运行，数据库和缓存承担基础支撑。浏览器端通过统一接口访问系统，普通业务采用 REST API，聊天消息先入库再通过 WebSocket 推送。这样的处理方式既保留了业务数据痕迹，也能改善实时沟通体验[8-15]。",
    201: "系统还需要处理好配置与运行环境切换。数据库连接、JWT 密钥、文件上传目录和模型脚本路径都不适合直接写死在代码中，而应放入配置文件或环境变量统一管理。这样在本地开发、测试部署和服务器运行之间切换时，代码主体不用频繁改动，维护成本也会更低[16-17]。",
    202: "系统的前后端分离架构如下图所示。浏览器端通过 Axios 调用后端 REST 接口，在需要实时沟通时再使用 WebSocket 完成消息交互。后端借助 Spring Security 与 JWT 完成身份校验，将通过校验的用户身份写入安全上下文，随后由业务服务处理预约、测评、聊天和后台管理等功能。数据库层负责保存结构化业务数据，模型分析模块则根据测评结果输出风险概率和等级判断[18]。",
    205: "系统总体上采用前后端分离模式。前端运行在浏览器中，并以 Vue 单页应用组织页面；后端运行在服务器侧，由 Spring Boot 对外提供接口服务；数据库负责保存平台业务数据。浏览器通过 HTTP 请求访问后端 REST API，在在线聊天场景下再与后端建立 WebSocket 连接[19]。",
    274: "登录认证与权限控制需要同时照顾前端使用体验和后端安全边界。用户登录成功后，前端保存 token，并根据角色跳转到对应首页，减少无关入口对用户的干扰；后端则通过 JWT 过滤器解析请求身份，再由服务层判断当前操作是否被允许。也就是说，前端负责把页面流程做顺，后端负责守住真正的权限边界，两者配合后才能形成较完整的访问控制链路[20]。",
    280: "后端登录流程由认证控制器接收用户名和密码，业务层再根据用户名查询用户记录，并使用 BCrypt 校验密码摘要。校验通过后，系统根据用户编号和角色生成 JWT，前端拿到 token 后写入 Pinia 状态和 localStorage。该方案把会话状态主要放在客户端保存，服务端接口通过过滤器解析请求头中的身份令牌，从而减少传统 Session 在前后端分离场景下带来的同步问题[21-23]。",
    289: "用户端功能实现的重点，是把高频入口放在用户容易找到的位置。用户登录后，可以从首页直接进入心理测评、预约咨询、心理文章和在线沟通，不需要在复杂菜单中反复跳转。对心理健康服务平台来说，入口清晰本身就是降低求助成本的一部分。尤其当用户正处在压力较大或情绪低落状态时，繁琐流程很容易削弱其继续使用的意愿[24-25]。",
    290: "在预约咨询功能中，用户需要先了解咨询师情况再作选择，因此页面会展示咨询师姓名、职称、擅长方向、从业年限、咨询价格、在线状态和评分等信息。用户提交预约时，需要填写预约时间、咨询形式、咨询时长和问题描述。系统保存预约记录后，将其置为待确认状态，等咨询师处理后再同步显示后续状态[26-31]。",
    425: "异常测试更关注系统在非理想条件下的反应。未登录用户直接进入业务页面时，系统应跳转到登录页；普通用户访问管理员页面时，前端路由或后端接口都应拦截；提交空测评答案、无效预约时间或不存在的咨询师编号时，页面应给出明确提示；WebSocket 断开后，历史消息仍应能通过接口加载。通过这些场景，可以判断系统是否只在正常流程下可用，还是具备一定健壮性[32]。",
    432: "从功能覆盖看，系统测试围绕三类角色和多条主流程展开，内容包括登录认证、心理测评、预约咨询、在线聊天、内容管理和数据看板。测试时不能只看按钮是否能够点击，还要核对前后端数据是否一致。例如，咨询师确认预约后，用户端应出现聊天入口；用户提交测评后，管理员端应能查看测评记录；文章发布后，用户端应能正常阅读[33]。",
    433: "从异常处理看，测试需要覆盖未登录访问、角色越权、参数缺失、数据不存在以及 WebSocket 断开等情况。普通用户进入管理员页面应被拦截，咨询师处理不属于自己的预约应被拒绝，聊天双方没有确认预约关系时也不能发送消息。对这些边界场景进行验证，可以检查系统是否能在出错时给出清晰反馈[34]。",
    450: "后续工作可以从移动端体验、模型管理和平台安全三个方向继续推进。学生使用心理服务时往往依赖手机，因此系统可进一步开发响应式页面或微信小程序。心理风险分析部分还需要补充数据来源说明、模型版本记录和人工复核流程。平台安全方面，则应继续完善操作日志、敏感数据访问审批和数据留存策略[35]。",
    451: "此外，平台后续还可以围绕心理健康素养提升、风险识别和数字化干预形成更完整的服务闭环[36-37]。在扩展咨询辅助、数据分析等功能时，仍需兼顾伦理边界、数据安全和服务质量。如果系统进入真实应用环境，还应与学校现有信息系统协同，例如统一身份认证、消息通知、学生基础数据平台和心理中心工作流。当前系统已经把核心业务拆分为较清晰的接口和数据表，后续接入外部能力时，可以在保留主体结构的基础上逐步扩展[38]。",
}


def rewrite_docx(src: Path, dst: Path) -> None:
    with zipfile.ZipFile(src, "r") as zin:
        files = {name: zin.read(name) for name in zin.namelist()}
    root = ET.fromstring(files["word/document.xml"])
    paras = body_paragraphs(root)
    changed = []
    for idx, text in REWRITES.items():
        if idx >= len(paras):
            raise IndexError(f"paragraph {idx} not found")
        before = para_text(paras[idx])
        replace_para_text_keep_style(paras[idx], text)
        changed.append((idx, before[:80], text[:80]))
    files["word/document.xml"] = ET.tostring(root, encoding="utf-8", xml_declaration=True)
    with zipfile.ZipFile(dst, "w", compression=zipfile.ZIP_DEFLATED) as zout:
        for name, data in files.items():
            zout.writestr(name, data)
    print(f"rewritten {len(changed)} paragraphs")


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("src", type=Path)
    parser.add_argument("dst", type=Path)
    args = parser.parse_args()
    rewrite_docx(args.src, args.dst)


if __name__ == "__main__":
    main()
